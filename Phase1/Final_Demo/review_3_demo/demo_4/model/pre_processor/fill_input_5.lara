aspectdef test
output stats end

stats = new LaraObject();

// fill input5.txt
//contains the function names, line number where the function starts, line number where the function ends

println("Start of Input5.txt");
select function end
apply
    if($function.name != "main")
    {
        println($function.name + " " + ($function.line-1) + " " + $function.endLine);
    }
    
end
println("End of Input5.txt");

end